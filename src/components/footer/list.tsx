import React from 'react';
import styled from 'styled-components';

import { Root as StackRoot } from '../../ui-lib/block/stack';

export const FooterList = styled(StackRoot)(() => ({
  margin: 0,
}));
